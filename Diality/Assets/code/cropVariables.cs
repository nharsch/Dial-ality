using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cropVariables : MonoBehaviour
{
    public bool grown;

    // Start is called before the first frame update
    void Start()
    {
        grown = false;        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
